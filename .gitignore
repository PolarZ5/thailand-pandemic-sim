node_modules/
.next/
out/
.vercel
.env.local
.DS_Store
*.log
*.tsbuildinfo

# Local Claude Code config (personal, not project state)
.claude/

# One-off download cache (Prem .rdata files, etc.) — not committed.
tmp/
